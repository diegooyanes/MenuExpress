<div class="auth-shell">
  <div class="auth-card">
    <div class="auth-panel">
      <div>
        <p class="hero-kicker">MenuExpress</p>
        <h2>Crea tu cuenta de restaurante</h2>
        <p>Te enviaremos un correo para verificar tu cuenta.</p>
      </div>

      <%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { class: "auth-form" }) do |f| %>
        <%= render "restaurants/shared/error_messages", resource: resource %>

        <div class="field">
          <%= f.label :name, "Nombre del restaurante" %>
          <%= f.text_field :name, autofocus: true, class: "form-control" %>
        </div>

        <div class="field">
          <%= f.label :email %>
          <%= f.email_field :email, autocomplete: "email", class: "form-control" %>
        </div>

        <div class="field">
          <%= f.label :password %>
          <% if @minimum_password_length %>
            <small class="text-muted">Minimo <%= @minimum_password_length %> caracteres</small>
          <% end %>
          <div class="password-field">
            <%= f.password_field :password, autocomplete: "new-password", class: "form-control" %>
            <button type="button" class="password-toggle" data-toggle="password" data-target="restaurant_password" aria-label="Mostrar contraseña"><span class="password-toggle-icon" aria-hidden="true"></span></button>
          </div>
        </div>

        <div class="field">
          <%= f.label :password_confirmation %>
          <div class="password-field">
            <%= f.password_field :password_confirmation, autocomplete: "new-password", class: "form-control" %>
            <button type="button" class="password-toggle" data-toggle="password" data-target="restaurant_password_confirmation" aria-label="Mostrar contraseña"><span class="password-toggle-icon" aria-hidden="true"></span></button>
          </div>
        </div>

        <div class="actions">
          <%= f.submit "Crear cuenta", class: "btn btn-primary" %>
          <%= link_to "Volver al inicio", restaurants_path, class: "btn btn-secondary" %>
        </div>
      <% end %>

      <div class="auth-links">
        <%= render "restaurants/shared/links" %>
      </div>
    </div>

    <div class="auth-aside">
      <div>
        <h3>Una plataforma hecha para restaurantes</h3>
        <p class="text-muted">Tu equipo trabaja con claridad y los clientes reservan en segundos.</p>
      </div>
      <ul>
        <li>Panel privado y seguro</li>
        <li>Reservas con estados claros</li>
        <li>Menu editable en minutos</li>
      </ul>
      <span class="pill">Prueba sin riesgo</span>
    </div>
  </div>
</div>
