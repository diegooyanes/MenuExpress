<div class="auth-shell">
  <div class="auth-card">
    <div class="auth-panel">
      <div>
        <p class="hero-kicker">MenuExpress</p>
        <h2>Recupera tu acceso</h2>
        <p>Te enviaremos un enlace para restablecer tu contrasena.</p>
      </div>

      <%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post, class: "auth-form" }) do |f| %>
        <%= render "restaurants/shared/error_messages", resource: resource %>

        <div class="field">
          <%= f.label :email %>
          <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "form-control" %>
        </div>

        <div class="actions">
          <%= f.submit "Enviar instrucciones", class: "btn btn-primary" %>
          <%= link_to "Volver al inicio", restaurants_path, class: "btn btn-secondary" %>
        </div>
      <% end %>

      <div class="auth-links">
        <%= render "restaurants/shared/links" %>
      </div>
    </div>

    <div class="auth-aside">
      <div>
        <h3>Seguridad primero</h3>
        <p class="text-muted">Tu cuenta y datos estan protegidos con acceso privado.</p>
      </div>
      <ul>
        <li>Recuperacion rapida</li>
        <li>Acceso seguro</li>
        <li>Soporte dedicado</li>
      </ul>
      <span class="pill">Ayuda inmediata</span>
    </div>
  </div>
</div>
