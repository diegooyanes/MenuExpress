<nav class="navbar">
  <div class="navbar-wrapper">
    <!-- Left: Logo -->
    <div class="navbar-left">
      <%= link_to "🍽️ MenuExpress", restaurants_path, class: "navbar-logo" %>
    </div>

    <!-- Center: Empty (for spacing) -->

    <!-- Right: Auth & Hamburger -->
    <div class="navbar-right">
      <div class="navbar-auth">
        <% if restaurant_signed_in? %>
          <span class="navbar-user"><%= current_restaurant.name %></span>
          <%= link_to "📅 Reservas", admin_restaurant_reservations_path(current_restaurant), class: "btn btn-sm btn-primary" %>
          <%= link_to "⚙️ Dashboard", admin_restaurant_path(current_restaurant), class: "btn btn-sm btn-secondary" %>
          <%= button_to "Salir", destroy_restaurant_session_path, method: :delete, class: "btn btn-sm btn-secondary", form: { style: "display: inline;" } %>
        <% end %>
      </div>
      <button class="navbar-burger" aria-label="Abrir menú" aria-expanded="false">
        <span class="burger-line"></span>
        <span class="burger-line"></span>
        <span class="burger-line"></span>
      </button>
    </div>
  </div>

  <!-- Mobile Menu Dropdown -->
  <ul class="navbar-menu">
    <li><%= link_to "Inicio", restaurants_path, class: "navbar-link" %></li>
    <li><%= link_to "Sobre Nosotros", about_path, class: "navbar-link" %></li>
    <li><a href="#contact" class="navbar-link">Contacto</a></li>
    <li class="navbar-divider"></li>
    <li class="navbar-section-title">Solo Restaurantes</li>
    <% if restaurant_signed_in? %>
      <li><%= link_to "📅 Gestionar Reservas", admin_restaurant_reservations_path(current_restaurant), class: "navbar-link" %></li>
      <li><%= link_to "⚙️ Dashboard", admin_restaurant_path(current_restaurant), class: "navbar-link" %></li>
      <li><%= link_to "+ Añadir Restaurante", new_admin_restaurant_path, class: "navbar-link" %></li>
      <li><%= button_to "Salir", destroy_restaurant_session_path, method: :delete, class: "navbar-link", form: { style: "text-align: left; border: 0; background: transparent; padding: 10px 18px; color: inherit;" } %></li>
    <% else %>
      <li><%= link_to "Iniciar Sesión", new_restaurant_session_path, class: "navbar-link" %></li>
      <li><%= link_to "Crear Cuenta", new_restaurant_registration_path, class: "navbar-link" %></li>
    <% end %>
  </ul>
</nav>
