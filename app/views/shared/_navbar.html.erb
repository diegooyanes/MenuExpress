<nav class="navbar">
  <div class="navbar-wrapper">
    <div class="navbar-left">
      <%= link_to restaurants_path, class: "navbar-logo" do %>
        <%= image_tag "Title.png", alt: "MenuExpress", class: "navbar-logo-image" %>
      <% end %>
    </div>

    <div class="navbar-center">
      <ul class="navbar-menu">
        <li><%= link_to "Inicio", restaurants_path, class: "navbar-link" %></li>
        <li><%= link_to "Sobre Nosotros", about_path, class: "navbar-link" %></li>
        <li><a href="#contact" class="navbar-link">Contacto</a></li>
        <li><%= link_to "Solo restaurantes", restaurants_landing_path, class: "navbar-link" %></li>
        <% if user_signed_in? %>
          <li><%= link_to "Mis reservas", users_reservations_path, class: "navbar-link" %></li>
        <% end %>
      </ul>
    </div>

    <div class="navbar-right">
      <% if restaurant_signed_in? %>
        <%= button_to "Cerrar sesión", destroy_restaurant_session_path, method: :delete, class: "btn btn-sm btn-secondary" %>
      <% elsif user_signed_in? %>
        <%= button_to "Cerrar sesión", destroy_user_session_path, method: :delete, class: "btn btn-sm btn-secondary" %>
      <% end %>
      <button class="navbar-burger" aria-label="Abrir menú" aria-expanded="false" <%= "data-role=\"sidebar-toggle\"" if controller_path.start_with?('admin/') && restaurant_signed_in? %>>
        <span class="burger-line"></span>
        <span class="burger-line"></span>
        <span class="burger-line"></span>
      </button>
    </div>
  </div>
</nav>
